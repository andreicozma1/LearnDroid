<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.webview">
    <!-- TODO: ADD INTERNET PERMISSION -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--        TODO: Add the below line inside application to enable ClearText traffic (http). Otherwise must use https -->
    <!--        android:usesCleartextTraffic="true"-->

    <!--    TODO: Define newly created activities inside AndroidManifest.xml-->
    <!--    ex: <activity android:name=".WebViewActivity_ex1" />-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WebViewActivity_ex7" />
        <activity android:name=".WebViewActivity_ex6" />
        <activity android:name=".WebViewActivity_ex5" />
        <activity android:name=".WebViewActivity_ex4" />
        <activity android:name=".WebViewActivity_ex3" />
        <activity android:name=".WebViewActivity_ex2" />
        <activity android:name=".WebViewActivity_ex1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>